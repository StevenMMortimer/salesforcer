#' @param report_filters \code{list}; To get specific results on the fly, filter 
#' reports through the API. Filter changes made through the API does not affect
#' the source report definition. Using the API, you can filter with up to 20
#' custom field filters and add filter logic (such as AND, OR). But standard
#' filters (such as range), filtering by row limit, and cross filters are
#' unavailable.
