#' @param line_ending character; indicating the The line ending used for CSV job 
#' data, marking the end of a data row. The default is NULL and determined by 
#' the operating system using "CRLF" for Windows machines and "LF" for Unix 
#' machines.
